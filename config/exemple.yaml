pipeline:
  input:
    type: csv
    file: data/test.csv
  steps:
    - filter:
        column: amount
        condition: ">1000"
    - group_by:
        column: category
  output:
    type: json
    file: data/result.json
