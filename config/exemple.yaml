pipeline:
  input:
    type: csv
    file: data/test.csv
  steps:
    - filter:
        column: amount
        condition: "> 500"
    - group_by:
        column: category
        aggregation:
          amount: "mean"
  output:
    type: json
    file: data/result.json
